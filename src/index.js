import React from 'react';
import ReactDOM from 'react-dom';
import './index.css.css'; // Eklediğimiz CSS dosyasını burada import edin
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
